# ADR 0309 — cargo-audit policy exceptions.
[advisories]
ignore = [
    # RUSTSEC-2024-0436: `paste` is unmaintained (no vulnerability).
    # Transitive via melior 0.27 → melior-macro → tblgen 0.9.1; no
    # upstream fix released yet. Re-evaluate on each melior upgrade.
    "RUSTSEC-2024-0436",
]
